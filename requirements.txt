pydantic>=2.5.0
orjson>=3.9.0
streamlit>=1.30.0
websockets>=12.0

# Secure credential storage (Phase 79)
keyring
argon2-cffi>=25.1.0

# Desktop dependencies
fastapi>=0.110
SQLAlchemy>=2.0
redis>=7.4.0
psycopg[binary]>=3.3.0
PyYAML>=6.0
ccxt>=4.0

httpx>=0.27,<1.0
anthropic

# Minimum version pin — see requirements-pinned.txt for full locked versions
anthropic>=0.50.0
