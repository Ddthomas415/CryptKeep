repos: []

# Root repo note:
# - This file is intentionally minimal.
# - This root file is the hook source of truth for the supported baseline.
# - No nested `crypto-trading-ai/` hook path is required for this repo.
# - If you use pre-commit locally, install it from the repo root.
# - Repo-level validation entrypoints remain the Makefile commands and
#   `python3 scripts/validate.py --quick`.
