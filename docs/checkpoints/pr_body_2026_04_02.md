# PR Body Draft 2026-04-02

## Summary

This PR publishes the current `followup/compat-cleanup` branch as the only currently verified green baseline for this workstream.

Two high-risk reviewed units were explicitly accepted before this PR:

- `2c4b8ce` `fix: restore runtime contract regressions for review`
- `092d6fc` `fix: tighten lifecycle and supervisor contracts`

Those accepted units are not replayable on top of `origin/followup/compat-cleanup` by themselves. A clean-branch reconstruction attempt showed that the remote baseline is missing required runtime prerequisites, including:

- `services/execution/lifecycle_boundary.py`
- `services/execution/coinbase_portfolio_guard.py`

Because of that, this PR should be reviewed as a coherent green branch with reviewed anchor commits, not as a narrowly isolated patch branch.

## Reviewed Anchor Changes

### Accepted unit 1: runtime contract restoration

Anchored by `2c4b8ce`.

- restored live-enable normalization in `services/execution/live_arming.py`
- restored reconcile/fetch contract behavior in `services/execution/live_executor.py`
- restored wrapper/raw-client reconciliation compatibility in `services/execution/order_reconciliation.py`
- restored Phase 1 auth compatibility in:
  - `phase1_research_copilot/gateway/main.py`
  - `phase1_research_copilot/orchestrator/main.py`
  - `dashboard/services/view_data.py`
- restored operator/diagnostics contract behavior in:
  - `scripts/op.py`
  - `dashboard/services/operator.py`
  - `services/admin/system_diagnostics.py`
- restored fail-closed warning payload behavior in `services/execution/place_order.py`
- corrected time-sensitive compatibility test data in `tests/test_remaining_compat_wrappers.py`
- restored `CHECKPOINTS.md` to canonical recent-phase tail shape

Review handoff:
- `docs/checkpoints/independent_review_handoff_2026_04_01.md`

### Accepted unit 2: lifecycle and supervisor contract tightening

Anchored by `092d6fc`.

- updated settings summary metrics in `dashboard/components/summary_panels.py` to reflect live notification channels, AI profile, and security posture
- changed evidence webhook stop behavior in `services/evidence/webhook_server.py` to return the concrete stop-file path
- routed order-manager cancel/replace cancellation through `lifecycle_boundary.cancel_order_async_via_boundary(...)`
- made `services/supervisor/supervisor.py` stop behavior explicit for the ops risk gate via `stop_risk_gate`
- updated lifecycle docs/tests to describe and enforce the active cancel/fetch boundary contract

Review handoff:
- `docs/checkpoints/independent_review_handoff_2026_04_02_dirty_slice.md`

## Verification

Main workspace proof:

```bash
./.venv/bin/python -m pytest -q
```

Result:

```text
1354 passed in 327.88s (0:05:27)
```

Additional targeted proof for the second reviewed unit:

```bash
./.venv/bin/python -m pytest -q tests/test_dashboard_operator_service.py tests/test_dashboard_summary_panels.py tests/test_order_manager_cancel_replace.py tests/test_supervisor_process.py tests/test_evidence_and_risk_recovery.py tests/test_execution_boundary_regression.py tests/test_live_execution_wiring.py tests/test_lifecycle_matrix_doc.py
```

Result:

```text
45 passed in 0.72s
```

## Review Guidance

Please review this PR with the following order of operations:

1. Review the accepted anchor commits first:
   - `2c4b8ce`
   - `092d6fc`
2. Use the handoff docs as the authoritative scope and risk summary:
   - `docs/checkpoints/independent_review_handoff_2026_04_01.md`
   - `docs/checkpoints/independent_review_handoff_2026_04_02_dirty_slice.md`
3. Treat the remaining branch history as dependency-bearing context required to preserve the verified green baseline.

## Known Limits

- No live exchange traffic was exercised.
- No end-to-end production deployment was exercised.
- No browser-driven UI flow was exercised end-to-end for protected Phase 1 routes.
- This PR description is honest about the branch shape: it does not claim that every commit in the ahead-of-origin history was independently reviewed.

## Why This Is Not a Smaller PR

A narrower PR was attempted and disproven. The accepted units could not be replayed cleanly onto `origin/followup/compat-cleanup` because the remote baseline lacks required runtime modules and the dependency chain extends beyond the reviewed patches themselves.

Publishing the current coherent branch is therefore safer and more honest than manufacturing a supposedly clean isolated branch that silently widens scope anyway.
