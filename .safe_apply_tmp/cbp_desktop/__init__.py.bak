# desktop/__init__.py - package init for desktop module

# Import all public classes/functions so they can be used as desktop.xxx
from .service_manager import (
# Safe fallback for service_registry (if not defined in service_manager.py)
    service_registry = None  # or implement basic registry if needed
    specs_default,
    start_service,
    stop_service,
    is_running,
    start_stack,
    stop_all,
)
from .logging_control import set_log_level, set_log_rotate_bytes, rotate_logs
from .panic import panic_stop_execution

# Optional: expose service_registry if it exists (from Phase 39+)
    service_registry = None  # fallback if not implemented yet

# Safe fallback for service_registry
    service_registry = None

# Safe fallback for service_registry
    service_registry = None

# Safe fallback for service_registry
try:
    from .service_registry import service_registry
except ImportError:
    service_registry = None
