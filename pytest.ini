[pytest]
testpaths = tests
norecursedirs = attic runtime data .venv build dist
addopts = -ra
markers =
    runtime: runtime and product behavior tests
    checkpoint: repo hygiene and checkpoint-format tests
    slow: tests that run real service loops (may block without a stop file)
    integration: tests requiring external services or live filesystem state
