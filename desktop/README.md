# Desktop

Reserved for Tauri shell (locked path).
Legacy python desktop code was archived into attic/.

Current repo-shape note:
- top-level `desktop/` is not the active implementation root
- desktop-related implementation currently exists elsewhere in the repo, including `src-tauri/`, `packaging/`, and `services/desktop/`
